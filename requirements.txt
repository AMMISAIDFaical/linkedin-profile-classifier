# --- Dev & Quality Tools ---
black==24.8.0          # last Black release that still supports Python 3.8
click==8.1.7
pytest==7.4.4
pytest-cov==4.1.0
pylint==2.17.7         # pylint 3.x wants newer runtime libs, 2.17.x is stable on py3.8

# --- API / Backend ---
fastapi==0.95.2        # FastAPI line that works well on Python 3.8
uvicorn==0.22.0        # compatible with that FastAPI and Python 3.8

# --- Scraping / Parsing ---
beautifulsoup4==4.12.3
lxml==5.3.0
requests==2.31.0
